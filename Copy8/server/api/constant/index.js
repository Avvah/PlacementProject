'use strict';

var express = require('express');
var controller = require('./constants.controller.js');

var router = express.Router();


//router.get('/name/:constantName', controller.getConstantByConstantName );

module.exports = router;
